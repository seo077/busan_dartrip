/**
 * 하단 데이터 출처 표기 — 전 화면 공통.
 *
 * 설계 정본: `API데이터설계.md` §10(출처 표기) · §1.1(소스 9종) / `화면구성도.md` §8
 *
 * 공공누리 이용허락 조건에 따른 **의무 표기**입니다. 화면을 새로 만들 때마다
 * 빠뜨리지 않도록 컴포넌트 하나로 묶어 둡니다.
 *
 * 여기는 짧은 목록이고, **무엇을 어디에 쓰는지까지 밝히는 자리는 S6 정보 화면**(`/about`)
 * 입니다. 순서는 ④ §10 의 문안을 따릅니다 — 한쪽만 고치면 어긋나므로 이 파일을 고칠 때
 * `app/about/page.tsx` 도 함께 봅니다.
 *
 * 여기 적는 것은 **서비스가 실제로 호출하는 데이터**뿐입니다
 * ---------------------------------------------------------
 * 공공누리 출처표시 의무는 활용한 데이터에 대한 것이므로, 상시 표기에 쓰지 않는 이름이
 * 섞이면 사실과 달라집니다. 기준은 하나입니다 — **호출하면 적고, 호출하지 않으면 뺍니다.**
 *
 *   · 한국관광 데이터랩 — 설계 단계에 저방문 구·군을 고르려고 한 번 참고했을 뿐 서비스가
 *     호출하지 않습니다. 여기 두지 않고 S6 의 "설계 단계 참고 자료" 로 성격을 갈라 적습니다.
 *   · 국가유산청 국가유산 공간정보 — 응답이 XML 전용이라 v1 에서 붙이지 않았습니다.
 *     호출하지 않으므로 이 목록에도, S6 에도 두지 않습니다. 보류 사유는 `README.md` §9 에만
 *     남깁니다(개발자가 읽는 자리). 안 쓰는 데이터를 화면에 적지 않는 편이 정확합니다.
 *   · 부산광역시 부산도보여행정보 — `scripts/backfill/07-walking.ts` 가 실제로 호출해
 *     `courses` 56건을 적재하고, 장소 상세의 "주변 도보 코스" 가 그 표를 읽습니다.
 *     활용하고 있으므로 여기 적습니다.
 *
 * 나중에 호출 여부가 바뀌면 그때 이 목록·S6·`README.md` §9 를 함께 옮깁니다.
 *
 * 표기 형식은 주최 측 공식 공지가 정합니다 (`D-43-3` · `NC-5`)
 * ----------------------------------------------------------
 * 공지가 예시로 든 올바른 표기는 **`출처: ⓒ한국관광공사`** 이고, **`TourAPI` 처럼 API
 * 서비스명만 단독으로 적는 것은 불가**입니다. 로고 이미지도 쓸 수 없어 **텍스트로만**
 * 적습니다. 그래서 각 줄을 `출처: ` 로 시작하고 기관명을 그대로 답니다.
 *
 * **`ⓒ` 는 한국관광공사 데이터에만 붙입니다.** 공지가 형식을 정한 것이 공사(및
 * 한국관광콘텐츠랩) 데이터이고, 나머지 기관에 같은 기호를 임의로 붙이면 공지가 정하지
 * 않은 표기를 우리가 만들어 내는 셈이 됩니다. 그쪽은 공공누리 표준대로 기관명만 적습니다.
 */

import Link from "next/link";

/** `출처: ` 뒤에 그대로 붙는 문자열. 형식 근거 = `NC-5`, 순서·목록 근거 = ④ §10 */
const DATA_SOURCES = [
  "ⓒ한국관광공사 — 국문 관광정보 서비스",
  "ⓒ한국관광공사 — 관광지별 연관관광지 정보",
  "ⓒ한국관광공사 — 두루누비 정보 서비스",
  "ⓒ한국관광공사 — 관광사진 정보",
  "부산광역시 — 부산명소정보",
  "부산광역시 — 부산도보여행정보",
  "행정안전부 — 전국모범음식점표준데이터",
  "카카오 — 카카오맵",
];

export function DataSources({ className = "" }: { className?: string }) {
  return (
    <footer
      id="sources"
      className={`scroll-mt-16 border-t border-line px-5 py-8 text-xs leading-relaxed text-ink-muted ${className}`}
    >
      <h2 className="mb-2 text-sm font-semibold text-ink">데이터 출처</h2>
      <p className="mb-2">본 서비스는 아래 기관이 제공하는 공공데이터를 활용합니다.</p>
      <ul className="mb-3 space-y-0.5">
        {DATA_SOURCES.map((source) => (
          <li key={source}>출처: {source}</li>
        ))}
      </ul>
      <p>
        위 데이터는 공공누리 이용허락 조건에 따라 출처를 표시하여 사용합니다. 데이터의
        최신성·정확성은 각 제공기관의 갱신 주기를 따르며, 실제 운영 정보는 방문 전 확인해
        주세요.
      </p>
      <Link href="/about" className="mt-3 inline-block underline underline-offset-2">
        서비스 소개와 데이터 출처 자세히 보기
      </Link>
    </footer>
  );
}

export default DataSources;
